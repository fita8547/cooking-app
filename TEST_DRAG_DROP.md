# 드래그 앤 드롭 & 재료 인식 테스트 가이드

## 구현 완료 사항

### 1. 드래그 앤 드롭 기능
- ✅ 이미지 업로드 모달에 드래그 앤 드롭 영역 추가
- ✅ `isDragging` 상태로 드래그 중 시각적 피드백 제공
- ✅ 드래그 이벤트 핸들러 구현:
  - `onDragOver`: 드래그 중 상태 활성화
  - `onDragLeave`: 드래그 영역 벗어날 때 상태 해제
  - `onDrop`: 파일 드롭 시 업로드 처리
- ✅ CSS 스타일링: `.upload-area.dragging` 클래스로 시각적 변화
  - 테두리 색상: #667eea (보라색)
  - 배경색: #e3f2fd (연한 파란색)
  - 약간의 확대 효과 (scale 1.02)

### 2. 재료 인식 기능
- ✅ OpenAI Vision API 통합 (백엔드)
- ✅ 이미지 → Base64 변환
- ✅ 재료 인식 결과 처리:
  - confidence > 0.5인 재료만 필터링
  - 인식된 재료 자동 추가
  - 사용자에게 결과 알림 (alert)
- ✅ 에러 처리 및 사용자 피드백
- ✅ 로딩 상태 표시 (Loader2 아이콘)

### 3. API 엔드포인트
- ✅ POST `/api/ai/recognize-ingredients`
  - 입력: `{ imageBase64: string }`
  - 출력: `{ ingredients: [{ name: string, confidence: number }] }`
  - OpenAI API 키 없을 시 목업 데이터 반환

## 테스트 방법

### 준비사항
1. 서버 실행: `npm run dev` (백엔드)
2. 프론트엔드 실행: `npm run dev` (프론트엔드)
3. 로그인 완료 상태
4. "어떤 재료가 있나요?" 페이지로 이동

### 테스트 시나리오

#### 시나리오 1: 드래그 앤 드롭으로 이미지 업로드
1. "사진으로 추가" 버튼 클릭
2. 이미지 파일을 드래그하여 업로드 영역으로 이동
3. **예상 결과**:
   - 드래그 중: 테두리가 보라색으로 변경, 배경이 연한 파란색으로 변경
   - "이미지를 여기에 놓으세요" 메시지 표시
   - 드롭 시: 재료 인식 시작 (로딩 표시)

#### 시나리오 2: 파일 선택으로 이미지 업로드
1. "사진으로 추가" 버튼 클릭
2. "사진 선택" 버튼 클릭
3. 파일 선택 대화상자에서 이미지 선택
4. **예상 결과**:
   - 재료 인식 시작 (로딩 표시)
   - "AI가 재료를 인식하는 중..." 메시지

#### 시나리오 3: 재료 인식 성공
1. 냉장고 사진 업로드 (김치, 두부, 대파 등이 보이는 사진)
2. **예상 결과**:
   - 인식된 재료가 자동으로 냉장실에 추가됨
   - Alert: "3개의 재료가 추가되었습니다: 김치, 두부, 대파"
   - 모달 자동 닫힘
   - 냉장실에 추가된 재료 표시

#### 시나리오 4: 재료 인식 실패 (재료 없음)
1. 재료가 없는 이미지 업로드 (예: 빈 냉장고, 풍경 사진)
2. **예상 결과**:
   - Alert: "재료를 인식하지 못했습니다. 다른 이미지를 시도해보세요."
   - 모달은 열린 상태 유지

#### 시나리오 5: 신뢰도 낮은 재료
1. 흐릿하거나 불명확한 이미지 업로드
2. **예상 결과**:
   - confidence > 0.5인 재료만 추가
   - 신뢰도 낮은 재료는 제외
   - Alert: "신뢰도가 높은 재료를 찾지 못했습니다..."

## 디버깅 팁

### 콘솔 로그 확인
```javascript
// FridgePage.jsx에서 확인 가능한 로그
console.log('인식된 재료:', ingredientNames);

// server/routes/ai.js에서 확인 가능한 로그
console.log('✅ OpenAI Vision API로 재료 인식 중...');
console.log('⚠️  OpenAI API 키 없음 - 목업 데이터 반환');
```

### 네트워크 탭 확인
1. 브라우저 개발자 도구 → Network 탭
2. POST `/api/ai/recognize-ingredients` 요청 확인
3. Request Payload: `imageBase64` 필드 확인
4. Response: `ingredients` 배열 확인

### OpenAI API 키 확인
```bash
# .env 파일 확인
cat .env | grep OPENAI_API_KEY

# 서버 재시작 (환경변수 다시 로드)
npm run dev
```

## 알려진 이슈 및 해결방법

### 이슈 1: 재료가 인식되지 않음
- **원인**: OpenAI API 키가 설정되지 않았거나 만료됨
- **해결**: `.env` 파일의 `OPENAI_API_KEY` 확인 및 업데이트

### 이슈 2: 드래그 앤 드롭이 작동하지 않음
- **원인**: 브라우저 기본 동작이 방지되지 않음
- **해결**: `e.preventDefault()` 및 `e.stopPropagation()` 호출 확인

### 이슈 3: 이미지가 너무 큼
- **원인**: Base64 변환 시 파일 크기 제한 초과
- **해결**: 이미지 압축 또는 리사이징 추가 (향후 개선)

## 다음 단계 개선사항

1. **이미지 미리보기**: 업로드 전 이미지 미리보기 표시
2. **이미지 압축**: 큰 이미지 자동 압축
3. **다중 이미지**: 여러 이미지 동시 업로드
4. **재료 수정**: 인식된 재료 확인 및 수정 UI
5. **카테고리 자동 분류**: 재료를 냉장실/냉동실로 자동 분류
6. **진행률 표시**: 업로드 및 인식 진행률 바

## 참고 파일

- `src/components/FridgePage.jsx`: 프론트엔드 UI 및 로직
- `src/services/openai.js`: OpenAI API 호출 래퍼
- `server/routes/ai.js`: 백엔드 AI 엔드포인트
- `vite.config.js`: API 프록시 설정
- `.env`: 환경변수 (OpenAI API 키)
