# 이메일 인증 설정 가이드

## 현재 상황

Resend API 키가 정상적으로 설정되어 있지만, **Resend 무료 플랜의 제한**으로 인해 이메일 전송이 제한됩니다.

### Resend 무료 플랜 제한사항

- `onboarding@resend.dev`에서 발송하는 이메일은 **인증된 이메일 주소로만** 전송 가능
- 현재 인증된 이메일: `junsumon090608@dgsw.hs.kr`
- 다른 이메일 주소로 전송하려면 **도메인 인증**이 필요합니다

## 해결 방법

### 방법 1: 개발 중 - 터미널에서 인증 코드 확인 (권장)

현재 코드는 이메일 전송이 실패하면 자동으로 터미널에 인증 코드를 표시합니다.

1. 서버를 실행합니다: `npm run server`
2. 회원가입을 진행합니다
3. 터미널에 표시되는 6자리 인증 코드를 확인합니다:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📧 [개발 모드] 이메일 인증 코드
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📨 수신자: user@example.com
👤 이름: 홍길동
🔑 인증 코드: 123456
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

4. 인증 코드를 입력하여 이메일 인증을 완료합니다

### 방법 2: 인증된 이메일로 테스트

`junsumon090608@dgsw.hs.kr` 이메일로 회원가입하면 실제 이메일을 받을 수 있습니다.

### 방법 3: 도메인 인증 (프로덕션 환경)

실제 서비스를 위해서는 도메인 인증이 필요합니다:

1. [Resend 대시보드](https://resend.com/domains)에 접속
2. 도메인 추가 및 DNS 레코드 설정
3. 도메인 인증 완료 후 `from` 주소를 인증된 도메인으로 변경

예시:
```javascript
from: 'AdCookingClass <noreply@yourdomain.com>'
```

## 현재 구현 상태

✅ 이메일 인증 시스템 완전 구현
✅ 6자리 인증 코드 생성 및 검증
✅ 인증 코드 재발송 기능
✅ 로그인 시 이메일 인증 체크
✅ 프론트엔드 인증 모달 UI
✅ Resend API 통합
✅ 이메일 전송 실패 시 터미널에 코드 표시

## 참고 자료

- [Resend 도메인 설정 가이드](https://resend.com/docs/dashboard/domains/introduction)
- [Resend API 문서](https://resend.com/docs/api-reference/emails/send-email)
